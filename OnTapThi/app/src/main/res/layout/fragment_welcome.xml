<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/screen_background"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="28dp"
        android:paddingEnd="28dp"
        android:paddingTop="56dp"
        android:paddingBottom="40dp">

        <!-- Hero Illustration -->
        <ImageView
            android:id="@+id/imgWelcomeHero"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:contentDescription="@string/welcome_title"
            android:src="@drawable/ic_illustration_study" />

        <!-- App name badge -->
        <TextView
            android:id="@+id/tvAppBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:background="#1A2196F3"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:text="@string/app_name"
            android:textColor="@color/welcome_primary"
            android:textSize="13sp"
            android:textStyle="bold"
            android:fontFamily="sans-serif-medium" />

        <!-- Title -->
        <TextView
            android:id="@+id/tvWelcomeTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:fontFamily="sans-serif-medium"
            android:text="@string/welcome_title"
            android:textColor="@color/text_primary"
            android:textSize="26sp"
            android:textStyle="bold"
            android:lineSpacingExtra="2dp" />

        <!-- Description -->
        <TextView
            android:id="@+id/tvWelcomeDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:fontFamily="sans-serif"
            android:text="@string/welcome_hero_description"
            android:textColor="@color/text_secondary"
            android:textSize="15sp"
            android:lineSpacingExtra="4dp" />

        <!-- Preview / personalized greeting -->
        <TextView
            android:id="@+id/tvWelcomePreview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:fontFamily="sans-serif"
            android:textColor="@color/welcome_primary_dark"
            android:textSize="14sp"
            android:textStyle="italic" />

        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:minHeight="40dp" />

        <!-- Primary: Bắt đầu -->
        <Button
            android:id="@+id/btnOpenHome"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="32dp"
            android:backgroundTint="@color/welcome_primary"
            android:fontFamily="sans-serif-medium"
            android:text="@string/action_get_started"
            android:textColor="@color/white"
            android:textSize="16sp"
            app:cornerRadius="14dp"
            style="@style/Widget.Material3.Button" />

        <!-- Secondary: Đăng nhập (outlined) -->
        <Button
            android:id="@+id/btnLogin"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="12dp"
            android:fontFamily="sans-serif-medium"
            android:text="Đăng nhập"
            android:textColor="@color/welcome_primary"
            android:textSize="16sp"
            style="@style/Widget.Material3.Button.OutlinedButton"
            app:strokeColor="@color/welcome_primary"
            app:cornerRadius="14dp" />

        <!-- Hidden fields kept for Java compatibility -->
        <EditText
            android:id="@+id/edtNickname"
            android:layout_width="1dp"
            android:layout_height="1dp"
            android:alpha="0"
            android:importantForAutofill="no"
            android:inputType="textPersonName"
            android:visibility="gone" />

        <Button
            android:id="@+id/btnSaveNickname"
            android:layout_width="1dp"
            android:layout_height="1dp"
            android:visibility="gone" />

    </LinearLayout>
</ScrollView>
